# README

> 使用 git@github.com:muyu0211/GoSearch.git 将代码拉至本地
 
项目直接提供了安装程序`GoSearch-amd64-installer.exe`，运行后可以直接安装；

如果想在本地构建，执行 `wails dev` 进行开发调试；

执行 `wails build` 进行项目构建。

## 注意

> 建议不要在应用程序运行期间修改配置文件内容(boot_config.json, config.json等), 可能会导致配置修改不生效。

